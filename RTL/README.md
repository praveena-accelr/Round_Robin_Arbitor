# Round Robin Arbitor module with .do file for Questa

- first run the simulation in console mode
```bash
vsim -c -do run.do
```
- next open the .wlf file and inspect the signals in GUI mode
```bash
vsim -view mywlf.wlf
```
